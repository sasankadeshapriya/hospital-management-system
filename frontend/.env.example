# Vite-specific port setting (used by Vite server)
VITE_PORT=port_number

# Frontend-specific environment variables
VITE_API_URL=http://api.example.com
VITE_API_KEY=yourapikey

# Backend-specific sensitive information (not accessible to the frontend)
DB_USER=yourDbUsername
DB_PASSWORD=yourDbPassword


#In your project code, access these variables like this:

# Frontend (e.g., in React components)
# const apiUrl = import.meta.env.VITE_API_URL;
# const apiKey = import.meta.env.VITE_API_KEY;

# Backend (e.g., in server code, if applicable)
# const dbUser = process.env.DB_USER;
# const dbPassword = process.env.DB_PASSWORD;